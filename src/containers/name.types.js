const Restaurant = "/";
export const RESTAURANT = {
    ROOT: `${Restaurant}`,
    DETAIL: `/restaurant/:secureId`
};

const Migrations = "/migrations"; 
export const MIGRATIONS = {
    ROOT: `${Migrations}`
};

const Cart = "/cart";
export const CART = {
    ROOT: `${Cart}`
};

const Transaction = "/transaction";
export const TRANSACTION = {
    ROOT: `${Transaction}`
};